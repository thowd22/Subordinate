[package]
name = "sub-model"
description = "Project data model: projects, sequences, tracks, clips, media items and bins"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sub-core = { path = "../sub-core" }
sub-time = { path = "../sub-time" }
serde = { version = "1", features = ["derive"] }
uuid = { version = "1.26", features = ["v7", "serde"] }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
